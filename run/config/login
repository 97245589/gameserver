include "include"

cluster_port = 10030
gate_port = 10031
server_type = "login"
server_name = server_type

thread = 2
if $DAEMON then
    logger = "run/" .. server_name .. ".log"
    daemon = "run/" .. server_name .. ".pid"
end