include "include"

server_name = "db"
server_id = "$PARAM"
server_mark = server_name .. server_id
cluster_port = "1005" .. server_id 

thread = 2
if $DAEMON then
    logger = "run/" .. server_mark .. ".log"
    daemon = "run/" .. server_mark .. ".pid"
end