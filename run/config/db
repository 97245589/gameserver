include "include"

server_type = "db"
server_id = "$PARAM"
server_name = server_type .. server_id
cluster_port = "1005" .. server_id 

thread = 2
if $DAEMON then
    logger = "run/" .. server_name .. ".log"
    daemon = "run/" .. server_name .. ".pid"
end